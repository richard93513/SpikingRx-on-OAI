<pre> # SpikingRx-on-OAI Debug Notes ## LDPC Decoder Bit-Exact Verification (rm_exact → LDPCdecoder → c[r]) Author: Richard Lin Project: SpikingRx-on-OAI --- # 0. 最終目標（替代 pipeline） ## 原始 OAI UE pipeline demapper LLR ↓ rate unmatching (nr_rate_matching_ldpc_rx) ↓ rm_exact (decoder input LLR) ↓ LDPCdecoder() ↓ c[r] (decoded codeblock) ↓ drop CB CRC ↓ concatenate CB ↓ drop TB CRC ↓ TB payload bits --- ## 我的替代 pipeline SpikingRx ↓ LLR ↓ rate unmatching ↓ rm_exact ↓ ldpctest_spx (LDPCdecoder) ↓ c[r] ↓ TB payload --- ## 本聊天室完成 LDPCdecoder stage **bit-exact alignment** rm_exact (UE dump) ↓ ldpctest_spx ↓ c[r] 與 UE 完全一致 --- # 1. Dump 檔案結構（bundle） 每個 bundle： spx_records/bundle/ fXXXX_sXX_idxXXXXXX_rntiXXXX_harqXX_rvX/ 包含： ldpc_cfg.txt rm_exact_seg00_i8.bin rm_exact_seg01_i8.bin ue_c_seg00.bin ue_c_seg01.bin ue_tb.bin --- # 2. 各 dump 檔案意義 ## rm_exact_segXX_i8.bin LDPC decoder 真正輸入 型態 int8 長度 out_len = (BG==1 ? 68×Zc : 52×Zc) 本例 BG = 1 Zc = 224 out_len = 68 × 224 = 15232 bytes --- ## ue_c_segXX.bin UE decoder output codeblock payload Kr_bytes = (K >> 3) - (F >> 3) - (C>1 ? 3 : 0) 本例 594 bytes --- ## ue_tb.bin UE 最終 TB payload A = 9480 bits TB = A/8 = 1185 bytes --- # 3. ldpctest_spx 功能 設計目標 input = rm_exact 只做 LDPCdecoder() 不能包含 rate unmatching 原因 避免 debug 混亂 --- # 4. Debug 過程 最初狀態 cmp_cb0 ≠ 0 cmp_cb1 ≠ 0 cmp_tb ≠ 0 懷疑 1 dump 錯 2 decoder 參數錯 3 output layout 不同 --- # 5. 發現的重要問題 UE decoder output 是 bytes path 不是 bit extraction + pack 因此 ldpctest_spx 必須 memcpy(llrProcBuf → c[r]) 而不是 bit extraction --- # 6. Buffer overflow bug 錯誤版本 K_bytes = 616 導致 malloc(): corrupted top size 原因 OAI decoder 寫超過 616 bytes --- # 7. 正確解法 使用 OAI 同等 buffer 27000 bytes 例如 llrProcBuf = memalign(32, 27000) 然後 memcpy(c_bytes[r], llrProcBuf, K_bytes) --- # 8. 成功驗證 執行 cmp cb_payload_seg00.bin ue_c_seg00.bin cmp cb_payload_seg01.bin ue_c_seg01.bin 結果 rc_cb0 = 0 rc_cb1 = 0 代表 LDPCdecoder stage bit-exact alignment --- # 9. 檢查指令 ## 進 bundle cd ~/SpikingRx-on-OAI/spx_records/bundle/&lt;bundle&gt; --- ## 確認檔案 ls -l rm_exact_seg00_i8.bin ldpc_cfg.txt ue_c_seg00.bin ue_c_seg01.bin ue_tb.bin --- ## 清舊輸出 rm -f cb_payload_seg*.bin tb_payload_bytes.bin decoded_bits.bin --- ## 執行 decoder ~/openairinterface5g/cmake_targets/ran_build/build/ldpctest_spx rm_exact_seg00_i8.bin ldpc_cfg.txt decoded_bits.bin --- ## 檢查 CB cmp -l cb_payload_seg00.bin ue_c_seg00.bin | head echo rc_cb0=${PIPESTATUS[0]} cmp -l cb_payload_seg01.bin ue_c_seg01.bin | head echo rc_cb1=${PIPESTATUS[0]} 成功條件 rc_cb0 = 0 rc_cb1 = 0 --- # 10. LLR sanity check python3 import numpy as np x=np.fromfile("rm_exact_seg00_i8.bin",dtype=np.int8) print("len",x.size) print("min",x.min(),"max",x.max()) print("mean",x.mean(),"std",x.std()) --- # 11. Binary build tag strings ~/openairinterface5g/cmake_targets/ran_build/build/ldpctest_spx | grep SPX --- # 12. Decoder 正確性結論 當 rm_exact identical 則 OAI LDPCdecoder ldpctest_spx LDPCdecoder bit-exact --- # 13. TB mismatch 原因 TB CRC = 24 bits = 3 bytes 594 + 594 = 1188 但 A/8 = 1185 差 3 bytes 因此 TB assembly 必須 copy until A/8 bytes --- # 14. 下一步 Step 1 修 TB assembly 讓 rc_tb = 0 Step 2 整合 rate-unmatching 使用 nr_rate_matching_ldpc_rx() --- # 15. 重要里程碑 已完成 rm_exact ↓ LDPCdecoder ↓ c[r] bit-exact alignment 這是 SpikingRx-on-OAI pipeline 最重要的 verification step </pre>
