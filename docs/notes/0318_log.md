# 🧠 SpikingRx-on-OAI 訓練問題完整筆記（最終整理版）

## 0️⃣ 核心目標（整個聊天室的主軸）

我們的最終目標是：

用 SpikingRx (SNN)  
取代 OAI RX pipeline：

```text
[fullgrid] → [LLR]
```

並且能接：

```text
LLR → rmunmatch → LDPC → decoded_bits
```

達到：

- BER ↓
- BLER ↓

---

## 1️⃣ 一開始發現的問題：訓練完全學不起來

### 現象

- `loss_llr ≈ 0.68`（幾乎不動）
- `sign ≈ 0.50`（隨機猜）

即使：

- bundle 從幾百 → 1939
- data pipeline 確認正常

### 👉 結論

- ❌ 問題不是資料量
- ❌ 問題不是 label 壞掉

---

## 2️⃣ 檢查 inference pipeline（確認 label 正確）

你做了這一串驗證（非常關鍵）：

```text
demapper_llr_f32.bin
→ rmunmatch_spx
→ ldpctest_spx
→ decoded_bits.bin
→ txbits.bin
```

並檢查：

- BER ≈ 0
- CB payload 完全一致

### 👉 結論

- LLR label 是正確的 ✅
- LDPC pipeline 是正確的 ✅

---

## 3️⃣ 發現核心矛盾（論文 vs 你的 pipeline）

你提出一個關鍵問題：

> 論文說取代 channel estimation + equalization  
> → 那 input 不可能是 equalized symbol

### 👉 推論

SpikingRx input = estimation 前的資訊（也就是 fullgrid）

這直接導致一個重要結論：

## 🚨 根因（第一次關鍵突破）

```text
fullgrid → LLR
```

是一個「過於困難的 mapping」

因為中間其實有：

```text
fullgrid
→ channel estimation
→ equalization
→ demapper
→ LLR
```

---

## 4️⃣ 嘗試增加 DMRS 資訊（第一個修正方向）

你做了：

### ✅ 新增 input channel

原本：2 channel（IQ）  
→ 改成：4 channel（IQ + DMRS）

並做：

- DMRS mask
- DMRS position 確認（你有用 dump 去看）

### 👉 結果

- 小資料 → 可以 overfit
- 大資料 → 還是學不起來

## 🚨 第二次關鍵結論

就算加 DMRS，model 還是學不起來

### 👉 表示

- ❌ 問題不是資訊不夠
- ❌ 問題是 mapping 太難

---

## 5️⃣ 決定導入「中間 supervision」

這是整個專案最重要的轉折點：

你提出：  
要不要加中間 supervision？

### 👉 我們決定

## 🔧 新 pipeline（multi-task）

input: `fullgrid`

model outputs:

- `y_llr`
- `y_eq`
- `y_ch`

loss：

```text
L = L_llr + w_eq * L_eq + w_ch * L_ch
```

---

## 6️⃣ 為了做 multi-task → 修改 OAI dump

這裡是你做的「系統級修改」👇

### 🔧 修改 1：dump equalized symbol

位置（你改過的）：

- `nr_dlsch_demodulation.c`

新增 dump：

- `ue_eqsymb.bin`

### 🔧 修改 2：dump channel estimation

同一支：

- `ue_chestext.bin`

### 🔧 修改 3：`bundle_records.py`

新增：

- `y_eq`
- `y_ch`
- `mask (data / dmrs)`

### 🔧 檢查結果

```text
[BUNDLE]
bundles_with_eqsymb = 1553
bundles_with_chestext = 1553
missing = 0
```

### 👉 結論

dataset 完整 ✅

---

## 7️⃣ Dataset sanity check

你印出：

```text
x.shape      = (1, 4, 14, 1272)
y_llr.shape  = (14400,)
y_eq.shape   = (2, 14, 1272)
y_ch.shape   = (2, 14, 1272)

mask:
eq_mask.sum  = 7200
ch_mask.sum  = 7200
dmrs_mask    = 600
```

### 👉 結論

resource grid 對齊完全正確 ✅

---

## 8️⃣ 第一次 multi-task 訓練結果

- `llr loss ≈ 0.68`（不動）
- `eq loss ≈ 34490`（超大）
- `ch loss ↓`（有下降）

### 👉 關鍵觀察

只有 channel 在學  
eq 完全沒學  
llr 完全沒學

## 🚨 第三次關鍵結論

multi-task（parallel）失敗

原因：

> model 沒有被強迫走 pipeline

---

## 9️⃣ 改成 serial pipeline（重大設計改變）

你改成：

```text
features
 → channel head
 → eq head（吃 channel）
 → llr head（吃 eq）
```

### 👉 這是對齊實際 RX pipeline：

```text
channel → equalization → demapper
```

### 🔧 同時修正 normalization（超關鍵）

- `y_eq` → normalize（mean=0 std=1）
- `y_ch` → normalize（mean=0 std=1）

---

## 🔟 訓練結果（serial + ch only）

- `ch loss ↓↓↓`（成功）
- `llr` 不動
- `eq = 0`（沒開）

### 👉 結論

```text
fullgrid → channel
```

已經學會 ✅

---

## 11️⃣ 打開 eq loss（最後實驗）

設定：

- `w_eq = 0.1`
- `enable_eq_loss = True`

### 結果

- `eq loss ≈ 1.00`（完全沒下降）
- `ch loss ↓`（OK）
- `llr` 不動
- `sign ≈ 0.51`（沒提升）

## 🚨 最終根因（現在已經非常清楚）

問題卡在：

```text
channel → equalization
```

具體來說：

- `eq supervision` 完全沒被學到
- → `llr head` 沒有有效 input
- → `llr` 永遠學不起來

---

## 🧠 最終 pipeline 狀態（現在）

```text
fullgrid → channel        ✅
channel  → eq            ❌（完全沒學）
eq       → llr           ❌（被卡住）
```

---

## 🚀 下一步 Debug 計畫（非常具體）

這邊是最重要的👇

### 🔥 Step 1：確認 eq target 本身「可學」

你現在要做：

### 👉 baseline（不用 SNN）

寫一個簡單 CNN / MLP：

- input: `channel_true`
- target: `eq_true`

### 👉 如果這個都學不起來：

→ `eq target` 有問題（定義錯 / scale錯 / mask錯）

---

### 🔥 Step 2：檢查 eq 定義是否正確

你要確認：

```text
ue_eqsymb = y / H ?
```

還是 MMSE eq？

### 👉 如果：

```text
eq target = MMSE
```

但 model 只看到 `H`

### 👉 那就是：

❌ 無法學（因為缺 noise power）

---

### 🔥 Step 3：加 noise power（超關鍵）

如果你現在 eq 是 MMSE：

```text
eq = y * conj(H) / (|H|^2 + N0)
```

### 👉 那你 model input 缺：

- `N0`（noise variance）

### 👉 必須 dump：

- `noise_power.bin`

---

### 🔥 Step 4：確認 eq mask / alignment

你要檢查：

```text
# 隨便抓一個 symbol
print(eq_true[mask])
print(eq_pred[mask])
```

看：

- 有沒有 phase flip
- 有沒有 scale mismatch
- 有沒有 shift

---

### 🔥 Step 5：分階段訓練（推薦）

改成：

#### Phase 1
只 train channel

#### Phase 2
freeze channel  
train eq

#### Phase 3
train llr

---

## 🧩 最終總結（一句話版）

你已經成功學會「channel estimation」，  
但現在卡在「equalization 無法學習」，  
導致整個 SpikingRx pipeline 無法往 LLR 傳遞。

---

## 如果你要我直接幫你下一步

我可以幫你直接做：

1. `eq-only baseline model`
2. 檢查 eq 定義（OAI code 對齊）
3. 幫你加 noise dump（直接給你改 `.c`）
4. 或直接幫你改 training script（分階段訓練版）

你下一句直接說：

```text
先做 eq baseline
```

我會直接給你完整可跑版本（照你 repo 結構）。
